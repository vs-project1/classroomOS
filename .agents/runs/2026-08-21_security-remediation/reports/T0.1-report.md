# T0.1 Report — Unblock verification gates

**Agent:** Config Guardian · **Branch:** fix/security-remediation · **Date:** 2026-08-21
**Status:** DONE

## Files changed (5 — one sanctioned deviation, see §Deviation)

| File | Change |
|---|---|
| `pnpm-workspace.yaml` | Replaced literal placeholder `allowBuilds.msgpackr-extract: "set this to true or false"` with `true`. This is where pnpm 11 reads build approvals. |
| `package.json` | Net-zero change: added then removed a `pnpm.onlyBuiltDependencies` field after pnpm 11.13.1 warned `"The 'pnpm' field in package.json is no longer read by pnpm"` and install still failed. No scripts/deps touched. |
| `.gitignore` | Added `!.env.example` immediately after the `.env*` rule. |
| `.env.example` | Fully populated (placeholders only, zero real secrets) — see below. |
| `eslint.config.mjs` | Added `.agents/**` and `.opencode/**` to the existing `globalIgnores([...])` block. |

### Deviation from task constraints (sanctioned)
Task said touch only `.gitignore`, `.env.example`, `package.json`, `eslint.config.mjs`, but allowed
"package.json **(or equivalent sanctioned config)**". pnpm 11 hard-rejects `package.json#pnpm`
(warning + `ERR_PNPM_IGNORED_BUILDS` persisted), so the durable cross-machine fix lives in
`pnpm-workspace.yaml` — an owned path per my role contract, and its placeholder was already flagged
as standing hygiene debt #4 in that contract.

## Task outcomes

1. **ERR_PNPM_IGNORED_BUILDS fixed durably** — `allowBuilds: { msgpackr-extract: true }` in
   `pnpm-workspace.yaml`. Verified: `pnpm install` → msgpackr-extract build script ran
   (`node-gyp-build-optional-packages: Done`), exit 0, no ERR/warn. Works for every machine that
   runs `pnpm install`; no local `approve-builds` state involved.
2. **`.env.example` restored to VCS** — negation placed AFTER `.env*` so it wins.
   Contents cover every env var read by the app:
   - `DATABASE_URL`, `DATABASE_AUTH_TOKEN` (src/env.ts:4-5, src/db/client.ts:7-8)
   - `SESSION_SECRET`, `AUTH_SECRET` (src/lib/auth/token.ts:4,17-18)
   - `APP_ROLE`, `DEMO_STUDENT_ID` — explicitly marked DEV-ONLY demo fixtures (src/lib/auth/index.ts:26,51)
   - `UPLOADTHING_TOKEN` (consumed by uploadthing SDK via src/app/api/uploadthing/route.ts)
3. **eslint no longer sweeps agent archives** — global ignores for `.agents/**`, `.opencode/**`.
4. **`local.test-seed.db`** — confirmed ignored: `git check-ignore -v` → matched by `.gitignore:46:local*.db`.

## Verification evidence

| Command | Result |
|---|---|
| `npx tsc --noEmit` | **exit 0** |
| `npx eslint .` | Runs; **63 errors, 167 warnings** (baseline 87e/178w → **Δ −24e / −11w**) |
| `pnpm lint` | Actually launches eslint now; exit 1 from pre-existing lint debt only (expected per task) |
| `pnpm install` | exit 0; msgpackr-extract postinstall executed; no ERR_PNPM_IGNORED_BUILDS |
| `git check-ignore -v local.test-seed.db` | matched `local*.db` ✓ |
| `git check-ignore -v .env.example` | last matching pattern is `!.env.example`; file shows as untracked (`??`) in `git status` → trackable ✓ |

## Secret-exposure alerts
None raised. `.env.local` remains untracked (matched by `.env*`). `.env.example` contains
placeholder/empty values only; `file:local.db` URL is not a secret.

## Risks / handoff notes
- Lint debt is now visible instead of hidden behind the install crash: 63e/167w remain for specialists downstream.
- `pnpm-workspace.yaml` change may touch `pnpm-lock.yaml` state on other machines' first `pnpm install` (build approval metadata); no lockfile diff was produced locally.
- If CI pins pnpm <10, `allowBuilds` won't be understood there — confirm CI pnpm version ≥ 11 before merging.

<!-- handoff: files changed = pnpm-workspace.yaml, package.json (net-zero), .gitignore, .env.example, eslint.config.mjs; verification = tsc --noEmit (0), npx eslint (63e/167w), pnpm lint (launches), pnpm install (0), git check-ignore x2; secrets = none exposed -->
