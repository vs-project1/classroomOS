@AGENTS.md

## Graphify Knowledge Graph
When exploring this codebase, ALWAYS consult the Graphify knowledge graph (run `uvx --from graphifyy graphify query 'your question'`) instead of using raw grep or finding tools.
Make sure to run the watch daemon via `npm run dev:watch` so the graph automatically stays up to date during development.
